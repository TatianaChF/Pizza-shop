import { render } from "@testing-library/react";
import {describe, expect, test} from '@jest/globals';
import Cart from "./Cart";

describe("Cart component", () => {
    test("calculation of the total amount of the order", () => {
        
    })
})